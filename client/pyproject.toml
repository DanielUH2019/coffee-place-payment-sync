[project]
name = "coffee-sync"
version = "0.1.0"
description = "Reliably propagate Coffee Place notebook payments (CSV) to the StarHarbour Central System."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "httpx>=0.27",
    "psycopg[binary]>=3.2",
    "tenacity>=8.2",
    "uvicorn[standard]>=0.30",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "respx>=0.21",
]

[project.scripts]
coffee-sync = "coffee_sync.cli:main"
coffee-sync-api = "coffee_sync.api:main"
coffee-sync-worker = "coffee_sync.worker:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["coffee_sync*"]

[tool.pytest.ini_options]
markers = [
    "integration: end-to-end tests that require the live Docker Compose stack",
]
testpaths = ["tests"]
